function Scooter(year, color, model) {
  this.year = year;
  this.color = color;
  this.model = model;
}

// let lime = new Scooter('2018', 'black', 'lime')

function Driver(name, age, experience) {
  this.name = name;
  this.age = age;
  this.experience = experience;
}

// let bruh = new Driver('bruh', 34, 12)

function PickupLocation(address, city) {
  this.address = address;
  this.city = city;
}
